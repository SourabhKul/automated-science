# Research ledger — 2026-09-26T0405Z wake

## Question and priority

The first Silverbox Qwen-plus-ABC pilot remains `unresolved/fit_incomplete`: after 512 attempts in the second ABC population, the linear hypothesis had 58/64 particles and the Qwen-selected `y_cubed` hypothesis had 42/64. Neither a validation score nor a sealed-test score exists. The highest-value next step is a separately identified v2 development run with a 2,048-attempt cap for **both** hypotheses, preserving the corrected reference kernel, fixed model, source, splits, priors, seeds, calibrated thresholds, validation rule, and failure gates. This is an exploratory budget adjustment informed by training-stage failure, not an independent scientific replication or a claim that Qwen discovered the device physics.

## Read-only preflight

A Luna operator fetched `origin` and found local `HEAD` and `origin/main` equal at `0bc1c80cb354babfcfaf1a7fe7bfcc27b9c31b92`, with a clean worktree before v2 edits. No new research artifacts had appeared since that commit. Process inspection found one `omlx-server` and no Qwen/ABC/Silverbox job; the v1 PID was absent. Its lingering lock-file path was not an active OS lock. The sole Silverbox run directory remains `artifacts/silverbox_first_fit/silverbox-20260925T191245Z-7e7d6af42abe/`, whose terminal stage says `unresolved/fit_incomplete` and has no selection receipt. A fresh `GET /v1/models` returned HTTP 200 with the exact `Youssofal--Qwen3.8-27B-MTPLX-Optimized-Speed` ID; no generation call was made. The official archive was 5,793,999 bytes with SHA-256 `2398dda503ac4c30f46c7a1ba09d8a8864d001cc71bbeb59f207a054cc592d8c`; the v1 receipts used about 388 KB. The host had 16 CPU cores, 128 GiB RAM, about 430 GiB free disk, and no competing research load. The active research-data set remains far below 50 GB.

## Implementation and independent review

The primary's proposed protocol is `reports/silverbox-second-development-protocol-proposal-2026-09-25.md`, ID `silverbox_first_fit_v2_2048_20260925`. Luna implemented an opt-in v2 ID/cap across fit, selection, orchestration, provenance, and the multisine-only final gate while retaining v1's 512-attempt behavior and historical receipts. A caller-prepared manifest must pin the reviewed Git commit and clean tree, exact source archive digest/size, code hashes, and Python/NumPy/psutil versions; the runner verifies those actual values before development loading and again before Qwen dispatch, copies the manifest bytes to ignored run artifacts, and chains their digest and a verified-preflight receipt through later stages. The v2 cap and ID must match in fit, population diagnostics, selection, terminal run, and final scorer. The final scorer still claims its single-use slot before sealed archive access and rejects unresolved runs.

Independent read-only review found one material pre-Qwen gap: the second check reused the first archive digest rather than hashing the current archive bytes. Luna added a fresh hash/size/path check immediately before proposal dispatch. A synthetic mutation-between-loader-and-Qwen test now ends unresolved with zero proposal calls. The scorer/runner reviewer approved the repair after **114** focused synthetic tests. A separate inference reviewer approved the v1/v2 attempt split and unchanged reference kernel after **113** focused synthetic tests, disclosing that they authored the original v1 fit module. The scorer reviewer authored the original scorer but not the v2 changes. Both reviews were read-only; no real Qwen request, ABC fit, validation selection, or final evaluation occurred during this wake so far. The reviewed implementation was synchronized as content commit `b0cbac4885dab84b9a43b8b1f75c68eb52704221` on `main`; a later status-only commit recorded this synchronization, and a fresh fetch verified that local `HEAD` and `origin/main` matched with a clean worktree. A real v2 manifest can now be frozen separately from the source sync.

## Decisions, limitations, and next gate

Preserve the original v1 incomplete run as historical evidence. Changes to the fit source hash can make historical v1 receipts fail current-code verification; do not weaken that gate or relabel old receipts as v2. The v1 protocol document's opening “no run” status describes its freeze-time state; this dated ledger records the subsequent v1 run. A v2 result with the same Qwen term and seeds is not an independent replicate. Call it a same-stream budget expansion only if the sampler and random-draw order match through the first 512 attempts; otherwise call it a same-seed follow-up. A different Qwen term makes cross-run fit comparisons descriptive only.

After the source sync, a separate operator should prepare the exact v2 manifest as an ignored immutable input, independently verify its contents and hash against the clean reviewed checkout and source/runtime, and only then launch **one** bounded v2 development run. If the fit or validation decision is unresolved, do not read the sealed multisine suffix. If selected, audit the frozen receipt and terminal status before the one allowed final score. No manuscript, external message, purchase, or financial transaction is authorized by this work.
