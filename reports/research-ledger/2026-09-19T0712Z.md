# Research ledger — 2026-09-19T0712Z

## Wakeup inspection

Read `AGENTS.md`, the initial scientific, code, and runtime audits, and the
current research state.  `HEAD` and `origin/main` were both
`4b8822ea8f39e052adf91f3d60eaa9602c634c36` after a fetch.  The only
uncommitted scope is the bounded Silverbox source audit.  A separate Luna
review is in progress; no model fitting, ABC-SMC run, Qwen generation, or
sealed evaluation was launched in this wakeup.

## Current milestone

The first real-data bridge is the public Silverbox nonlinear-system benchmark.
It is a **methods and provenance milestone**, not a scientific-discovery
experiment.  The audit reports a 5,793,999-byte archive, 9,738,543 expanded
bytes, and the archive SHA-256
`2398dda503ac4c30f46c7a1ba09d8a8864d001cc71bbeb59f207a054cc592d8c`.
That is comfortably inside the 50 GB active-data limit.  Its contract keeps
the official test outputs sealed except for the documented 50-sample state
initialization window, and identifies the arrow test subset so it cannot be
counted as an independent confirmation.

The public source does not state dataset reuse terms.  This blocks
redistribution and any public benchmark/publication claim until clarified; it
does not prevent retaining an internal source-audit record.

## Decision and next gate

An independent Luna reviewer must verify the source contract, candidate-view
boundary, artifact ignores, and focused tests.  If approved, a single Luna
Git owner will inspect the remote, commit only this reviewed scoped diff,
push without force, and verify the content commit on `origin/main`.  Only
after that will the project pre-register a validation split *within* the
published development record and specify a small, fixed real-data inference
control.  No test outcome may affect that choice.
