# Research ledger — 2026-09-24T2250Z

## Read-only status and review

The independent Silverbox final review approved the source adapter, focused
tests, manifest, split contract, archive hashes, and development/scoring
boundary. The reviewer reported seven focused tests passing. No implementation
changes were required for synchronization.

At 22:50 UTC, `http://127.0.0.1:8000/v1/models` refused the connection, so
availability of the fixed model `Youssofal--Qwen3.8-27B-MTPLX-Optimized-Speed`
could not be confirmed. The research-process scan found no active ABC,
Silverbox, science, oMLX, Qwen, fitting, proposal, experiment, or research
jobs. No fitting, ABC-SMC, Qwen generation, or sealed evaluation was launched
in this synchronization task.

Before synchronization, local `HEAD`, local `origin/main`, and remote
`origin/main` all resolved to
`4b8822ea8f39e052adf91f3d60eaa9602c634c36`. The raw archive remains under the
ignored `data/real/silverbox/raw/` path. The public source still states no
dataset license or reuse terms, and the reviewed adapter uses CSV values that
differ numerically from the archived MAT files; both limits are recorded in
the source audit.

## Decision and next gates

The unavailable runtime is recorded as a separate Qwen-experiment gate. It is
not required to synchronize the independently reviewed offline adapter. The
scientific sequence is in
[`real-data-discovery-next-milestone-2026-09-24.md`](../real-data-discovery-next-milestone-2026-09-24.md): synchronize the source contract; verify the fixed Qwen endpoint before generation; freeze development choices and the scoring contract; run one bounded real-data integration pilot; lock and score once.

The five-hour automation is active on this task with `gpt-6-luna` at maximum
reasoning. Delegated engineering, review, tests, experiment execution,
monitoring, and Git synchronization use that allocation. Research runs remain
restricted to the fixed local Qwen model.
