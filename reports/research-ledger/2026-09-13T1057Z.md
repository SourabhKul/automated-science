# Bounded Gaussian ABC-SMC reference checkpoint: 2026-09-13 10:57 UTC

The exact local model preflight was repeated once with a read-only `GET http://127.0.0.1:8000/v1/models`. It returned HTTP 200 and advertised `Youssofal--Qwen3.8-27B-MTPLX-Optimized-Speed`; no chat completion or other generation request was made. No alternate model or paid resource was used.

The historical `core/sbi_engine.py` implementation remains untouched. The new opt-in `core/abc_smc_reference.py` callable requires an explicit non-increasing epsilon schedule and finite attempt budgets. For every proposal attempt after generation zero it samples a fresh ancestor and Gaussian perturbation, rejects proposals outside support without clipping, and evaluates the same fixed regularized covariance in both `Generator.multivariate_normal` and the unconditioned mixture log density. Accepted transition particles receive stable log prior-over-mixture weights, with normalized weights and ESS returned for every population. Simulation, discrepancy, support, weight, and budget failures are counted; an incomplete population is returned with an explicit status and termination reason rather than a prior-generation substitute.

`core/abc_reference_controls.py` provides an independent fixed Gauss-Legendre quadrature target for the finite-epsilon scalar control and weighted empirical mean, quantile, CDF, ESS, and Monte Carlo scale diagnostics. `tests/test_abc_reference.py` covers normalization, saved mixture densities, support rejection without boundary atoms, weighted summary conventions, general-support dimension inference, bounded failure, and all six scalar cells (`y={0.1,0.5,0.9}`, `epsilon={0.1,0.025}`). The test-side target is recomputed with separately coded quadrature so the implementation helper is not its only source of truth.

The single-population rejection baseline in `reports/abc-reference-control-pilot-2026-09-13.json` and `.md` used three seeds (`11, 29, 47`), 400 accepted particles per cell, and 50,000 attempts per cell; all 18 cells completed in 1.229 seconds. To exercise sequential weighting and support rejection, the same bounded driver was then run with the explicit two-population schedule `[0.2, epsilon]`. The sequential JSON/Markdown metrics are `reports/abc-reference-sequential-control-pilot-2026-09-13.json` and `.md`; all 18 cells completed in 2.519–2.530 seconds across identical bounded runs, persisted both population receipts (including normalized weights and generation-1 covariance), and recorded transition out-of-support retries. ESS-scaled errors are descriptive Monte Carlo diagnostics and are not formal confidence statements for dependent weighted SMC particles. A small ODE stage was not started because the scalar sequential milestone was sufficient for this bounded slice.

Independent review approved the scoped reference, tests, sequential evidence, and documentation. The designated operator may now perform the single-owner synchronization step. Git maintenance policy remains: synchronize only completed reviewed changes, keep raw/bulk artifacts and secrets out of Git, and never force-push.
