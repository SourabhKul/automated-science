# ODE-control recovery checkpoint: 2026-09-17 10:11 UTC

This checkpoint records the first bounded recovery after the 2026-09-15
workspace-credit failure. It does not record a pilot or a Git synchronization.

The checkout was inspected before work. Local `HEAD` and `origin/main` were
both `e6c6008b19628fcb7d57ca4c79b91ae1a5d6f79d` (`Persist canonical Qwen
response provenance`), the worktree had no tracked changes before this
recovery, and no ODE, ABC, pilot, or checkpoint process was active. A fresh
read-only `GET http://127.0.0.1:8000/v1/models` returned HTTP 200 and
advertised the exact fixed model
`Youssofal--Qwen3.8-27B-MTPLX-Optimized-Speed` (alongside two other advertised
models). No chat-completions request or other LLM generation was made.

The preserved partial implementation was inspected and completed enough for
focused review in `core/ode_reference_controls.py` and
`tests/test_ode_reference_controls.py`. The control uses fixed-step RK4 from
the original `t=0`, with predeclared `max_step=0.05`, `max_steps=100000`, and
an independent solver-error acceptance tolerance of `1e-6`. The test-side
target independently recomputes the df=4 noncentral-chi-square Poisson mixture
and Gauss-Legendre normalization. Tests also cover fresh simulator noise and
separate proposal state, unequal-weight latent versus observation-predictive
summaries, the single-future-time origin, transition-density/weight
recomputation, forced solver failure, and an unattainable discrepancy with a
finite attempt budget.

Observed checks at this checkpoint:

```text
python3 -m py_compile core/ode_reference_controls.py tests/test_ode_reference_controls.py  PASS
python3 -m compileall -q core/... tests/test_ode_reference_controls.py              PASS
python3 -m tests.test_ode_reference_controls                                      PASS
python3 -m tests.test_abc_reference                                               PASS
git diff --check                                                                   PASS
```

The focused ODE test and existing ABC reference test both exited 0. The
independent reviewer is inspecting this exact uncommitted diff. The six-cell
ODE pilot has not been launched; no pilot receipt, target agreement, or
scientific inference claim is made. The serial driver and atomic receipt
configuration remain pending review. The previous failure checkpoint remains
at `reports/research-ledger/2026-09-15T2258Z.md` and is preserved as history.

During review, an independent blocker was found in the first draft's central
chi-square calculation: it had used `x` instead of the required Gamma argument
`x/2`. The implementation and separately written test helper were corrected.
The optional direct `scipy.stats.ncx2` regression now covers thresholds 4 and 9
with noncentralities 0, 0.5, 8, and 40; an offline review run matched all
values to at most `3.3e-16`. This correction was applied before any stochastic
pilot, and the reviewer has been asked to recheck the exact diff.

The subsequent review also required an explicitly separate ordinary
Gaussian-likelihood reference. The draft now exposes
`GaussianLikelihoodODETarget`/`gaussian_likelihood_ode_target`, with density
proportional to `exp(-lambda/2)` from independent quadrature, while the pilot
target remains the finite-epsilon noncentral-chi-square acceptance target.
Both target densities are zero outside `[0.05, 1.5]`; focused tests assert
that their means and quantiles differ for the control dataset rather than
requiring exact-likelihood agreement.

The reviewer-required follow-up is now implemented in the same uncommitted
scope and remains pending independent re-review. Incomplete cells gate
`parameter_diagnostics` and `particle_prediction` to explicit unavailable
records with `posterior_summary: false`; a regression uses target size 3,
epsilon `[8]`, and one attempt. The serial driver declares the six cells,
separate data/proposal/simulator seeds, the `[3, 2]` epsilon schedule, 400
particles, 50,000 attempts per population, and a configurable wall cap no
greater than 600 seconds. It writes atomic per-cell receipts and an atomic
aggregate partial receipt, recording per-population elapsed time and solver
failure categories. One absolute monotonic deadline is passed from the
driver into each cell and then as `stop_requested` into the reference
proposal-attempt loop; it is therefore enforced during a population. The
driver has not been launched, so no pilot results or scientific claims exist.

Post-change checks remain passing: the focused ODE tests, existing ABC
reference tests, offline SciPy cross-check, bytecode compilation, help-path
check, and `git diff --check`. No ODE/ABC/pilot process is active. The code
and this ledger are uncommitted and frozen pending the reviewer verdict; no
Git synchronization has occurred.

The reviewer then required a final wall-cap edge-case fix. The reference
proposal loop now checks the deadline immediately after each simulator call,
including the simulator-exception path, before discrepancy evaluation or
acceptance. A sleeping-simulator regression uses a 20-ms deadline and a
50-ms call and verifies `incomplete`, `wall_budget_exhausted`, one simulated
attempt, and zero accepted particles. The focused ODE and ABC tests,
compilation, and `git diff --check` were rerun successfully. No pilot or
commit was made; reviewer recheck is requested.
