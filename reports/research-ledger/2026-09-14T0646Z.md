# Bounded Qwen response-provenance checkpoint: 2026-09-14 06:46 UTC

This checkpoint records the reviewed canonical completion-provenance milestone. The fixed research model was `Youssofal--Qwen3.8-27B-MTPLX-Optimized-Speed` at `http://127.0.0.1:8000/v1`; the preflight at 06:54:52Z returned HTTP 200 and listed that exact model. Only the pre-existing `omlx-server` was active. The implementation keeps the legacy `call_llm` string-or-`None` API and adds canonical-only per-request receipts with status, HTTP code, error, finish reason, usage, elapsed time, timestamps, raw response, and raw-response SHA-256. Receipts are atomically persisted after every proposal or repair, including failures. The focused post-patch suite passed 24 tests, the legacy configuration script passed, and compile/diff checks passed.

The live run used a new synthetic 61-point ecology seed fixture and the explicit `trajectory_train_validation_sealed_v1` development manifest. Its run ID is `canonical_qwen_response_provenance_smoke_20260914T0657Z`. The declared budget was two proposals, one global repair, `max_tokens=2048`, `enable_thinking=false`, `initial_particles=32`, `target_samples=8`, `generations=2`, and 120-second candidate/request timeouts within a 600-second wall bound. It made exactly two proposal requests and no repair requests. Both were HTTP 200 with `finish_reason=stop`, no errors, and persisted raw bodies:

| stage | elapsed (s) | completion tokens | total tokens | raw response SHA-256 |
| --- | ---: | ---: | ---: | --- |
| proposal 1 | 14.417918 | 320 | 2158 | `68e77778a69815c010e72711d3a1e2cce5849c1b0e0ea474dcdf98a2a2cf2a69` |
| proposal 2 | 15.276697 | 354 | 2427 | `134ca5bdf86b442fbde2354febf80cf66ff60fe95b18fdaaf4f0a09ef2942640` |

Both candidate evaluations were finite. Proposal 1 was accepted with development validation MSE `1.9346842034702103` and median distance `220.64639282226562`; proposal 2 was rejected with validation MSE `4602.476561283648`. The coarse development wall interval was 36.5635 seconds; the two Qwen requests summed to 29.6946 seconds and spanned 32.0226 seconds. No final outcomes entered the search receipt (`final_outcomes_available_to_search=false`, `final_data_hash=null`).

The separate frozen final evaluator exited 0 without a model request. It produced a finite terminal receipt with MSE `9.470892248597332`, RMSE `3.077481478189159`, and 26 observed values. The final receipt SHA-256 is `ec89484e1315db3bb7317272e96133a5b46df67955d762d4b3e8869fcc3794aa`.

Durable ignored evidence, including the synthetic recipe, manifests, prompts, raw response receipts, candidate diagnostics, frozen selection, final receipt, endpoint/process snapshots, summary, and 32-file SHA-256 inventory, is at `artifacts/evaluation_boundary/canonical_qwen_response_provenance_smoke_20260914T0657Z/`. The artifact-manifest SHA-256 is `eff6749a6464399c13024e9db61ff1ce9c47173cb1dbadce68f81066d3eec3dc`. This is transport, provenance, and bounded synthetic protocol evidence only; it is not a scientific discovery or generalization claim.
