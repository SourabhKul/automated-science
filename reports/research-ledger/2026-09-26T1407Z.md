# Research ledger — 2026-09-26T1407Z wake

## Decision and scientific claim boundary

The highest-value unblocked milestone is a trustworthy second-system
Cascaded Tanks **methods control**, not a benchmark leaderboard claim or new
tank physics. The preceding Silverbox multisine final is consumed after a
terminal 3/64-particle nonfinite failure with no final RMSE. Preserve it.
The tank official `uVal/yVal` test remains unused. Its estimation record is
the sole source for development; the older multi-record external-repeat gate
still fails. The official source describes overflow and an uncalibrated
sensor, so a ceiling in the training output cannot by itself identify a
physical overflow or sensor law. Relevant primary sources are the
[original benchmark manuscript](https://cris.vub.be/ws/portalfiles/portal/24190624/CascadedTanks_Maarten.pdf),
[official benchmark](https://www.nonlinearbenchmark.org/benchmarks/cascaded-tanks),
and [official loader source](https://github.com/MaartenSchoukens/nonlinear_benchmarks/blob/f9fb3883086870a27b31917ccde1f78c95d53cb2/nonlinear_benchmarks/benchmarks.py#L113-L136).
The [ABC model-choice caveat](https://arxiv.org/abs/1102.4432) precludes
calling finite-summary particle weights a Bayes factor for mechanisms.

The primary drafted
`reports/cascaded-tanks-inference-design-proposal-2026-09-26.md` for a
discrete-time voltage-equivalent S0 baseline and two competing idealized
lower-state-cap (O2) and sensor-ceiling (C2) surrogates. Qwen would choose
one testable augmentation from a fixed grammar or abstain, using only
training outputs and optionally forecast inputs. These are **not** the
published exact overflow/sensor equations; the pilot omits their stochastic
effects. An independent Luna scientific review found a scale symmetry in
the initial sketch and confirmed that fixing the latent-to-output inflow
gain to 1 removes that exact gauge. The primary corrected the design and
retained practical identifiability as unresolved. Review also required
cross-family no-crossing and crossing/recovery synthetic controls, explicit
state-only final initialization, exact input/output index alignment, and
deferred-target wording; these corrections are in the proposal. Numeric
priors, ABC tolerances, validation/refit/final rules, run ID, and manifest
remain **unfrozen**. No real-data Qwen or ABC run can start yet; later
synthetic ABC glue tests in this wake do not clear that gate.

## Operational and boundary evidence

A Luna read-only preflight fetched `origin` and found clean `main ==
origin/main` at `288cbc3e543cdb73a02fb82a8c9b16621b6d4956`, zero ahead/
behind. `GET http://127.0.0.1:8000/v1/models` advertised the exact fixed
`Youssofal--Qwen3.8-27B-MTPLX-Optimized-Speed` ID; no chat request was
made. `omlx-server` PID 15407 was the only relevant running process; no
Python/ABC research job appeared. An old Silverbox lock contains PID 62852,
which no longer exists; no process held that lock or the empty final lock.
The operator left them untouched. Host: 16 CPUs, 128 GiB RAM (97% free at
check), 431 GiB disk free, low load. Workspace `data` 6.2 MiB and
`artifacts` 56 MiB; the already acquired tank archive is 7,520,592 bytes,
far below the 50 GB active-set cap. The operator did not launch a run.

An independent Luna source-boundary review found no direct target leak in
`core/real_data/cascaded_tanks_controlled.py`: it buffers/converts only
allowed `uEst`, training `yEst[0:768]`, and `Ts`; synthetic sentinel tests
alter hidden columns and the development target suffix without altering the
development view or receipts. The review identified an in-process
constructor token that is importable, so it is **not cryptographic source
provenance**. Future scorers must independently verify the pinned archive
and receipt chain. The current adapter has no official test accessor,
deferred-target scorer, or target-materialization failure test. A future
test accessor may read `uVal` and only `yVal[0:50]` for declared latent-state
initialization, skip later target bytes, and reopen after complete forecasts;
none of that has been implemented or exercised. This is a procedural blind
over public data, not secrecy.

## Synthetic-only implementation in progress

One Luna operator created an isolated S0/O2/C2 simulator and synthetic tests
in `core/real_data/cascaded_tanks_models.py` and
`tests/test_cascaded_tanks_models.py`. The primary's read-only inspection
requested additional no-threshold and crossing/recovery **observable**
controls, beyond an initial hidden-state memory check. An independent Luna
code review found no numerical/alignment defect but identified an absent
direct dry-floor activation test and the need to state O2's `x2_0 <= H`
support restriction. The primary documented that restriction; the author
added hand-calculated no-crossing, crossing/recovery, and dry-floor tests.
The author reported **20 focused tests**, Ruff, formatting, compilation,
and diff-whitespace checks passing. The separate reviewer examined the final
floor test arithmetic and cleared **synthetic-only scoped sync**, but could
not rerun tests because Pytest/Ruff were absent from its current PATH. This
does not approve the component for real-data fitting. No source adapter,
legacy inference file, real data, Qwen, ABC, or Git was touched by this code
operator. A separate caller-array ABC glue bridge was implemented and
reviewed later in this wake; it is described below.

## Later in wake: independently reproduced training-only ceiling diagnostic

Two Luna operators used fresh calls to the reviewed source adapter, which
returned only 768 training input/output pairs and 256 later **inputs**.
Both reproduced 27 exact 10.0 V training outputs in one maximal run
`[150,176]` (108 seconds); the declared closed `9.95–10.05` V band has 28
points in `[150,177]` (112 seconds). No training output exceeds 10.0 V.
The training-input Q25 is 2.053 V using sorted linear interpolation
`h=(768−1)*0.25=191.75`. Among 11 maximal `u<=2.053` runs, one qualifies
under the declared rule: an output at least 9.95 V in the 16-sample lookback
including the run onset, followed by an output below 9.5 V while low input
continues. That input run is `[178,191]` (14 samples); the first value
below 9.5 is at index 185, 7 samples after onset. The later **input-only**
segment has 54/256 values at/below training Q25 and 28/256 below the
training-input minimum. All counts, intervals, quantiles, and the source,
adapter, contract and stage hashes matched between operators. Their passes
took 0.022 and about 0.028 seconds, with about 27 MB peak RSS; no new data
copy, hidden output, fit, simulation, Qwen, ABC, or Git action occurred.

This establishes one observed ceiling-like plateau and one following
low-forcing decay, not its cause. The plateau is far from the proposed
development boundary at 768, so the future input may not expose the O2/C2
memory contrast. The design proposal now requires a pre-target forecast-
separation diagnostic and an explicit **unresolved** structural result if
the fitted families cannot be distinguished under a training-only criterion.
Even a later predictive score would not identify sensor clipping versus
tank overflow from this one correlated voltage record.

## Later in wake: checked-reference ABC glue on synthetic controls

A second Luna coder added `core/real_data/cascaded_tanks_synthetic_abc.py`
and `tests/test_cascaded_tanks_synthetic_abc.py`, without changing the
official source adapter, simulator, or legacy SBI runner. It connects the
S0/O2/C2 simulator to the opt-in Gaussian ABC-SMC reference for
**caller-supplied arrays of unverified origin**. Intended use is synthetic
integration control, not a real-data run or a source-isolation security
barrier. Free parameters are sampled uniformly in a normalized unit cube;
the declared support/log density and Gaussian-mixture proposal operate in
that same measure, then an affine decoder supplies effective surrogate
parameters. For this affine uniform prior, the constant physical-coordinate
Jacobian cancels in relative weights. Fixed/free parameter coverage, the
O2 `x2_0<=H` rectangular-support rule, finite full-length trajectories,
all-trajectory RMSE, categorized simulator failures, attempt caps, and
no-terminal-posterior on incomplete populations are explicit.

The author reported **3 focused synthetic tests** plus Ruff, formatting,
compilation, and whitespace checks passing. One test uses an independent
test-side S0 loop on a one-free-parameter known-setting slice and
independently recomputes generation-one scalar Gaussian-mixture weights;
another forces all attempts to fail and checks no posterior promotion;
the third checks seeded replay. An independent Luna inference reviewer
reran the focused 3 tests and Ruff, checked the sampler/log-density/support,
affine map, proposal weights, discrepancy, and failure semantics, and found
no mathematical blocker. The primary caught that the initial provenance
called arbitrary arrays synthetic; the author corrected it to
`caller_supplied_unverified_arrays`, `synthetic_origin_verified=false`,
`has_fixed_parameters`, surrogate-coordinate bounds, and an explicitly
intended glue-control evidence scope. A separate Luna reviewer confirmed
those final metadata claims after the patch. There is no O2/C2 ABC recovery
control, broad parameter-calibration evidence, wall deadline, or real-data
bridge yet; none is claimed. This module is cleared only for compact
synthetic-control synchronization.

## Next gates

Synchronize only reviewed compact code/report files via one Luna Git owner.
Separately implement and check a source-gated real-data ABC wrapper and
deferred development scorer with broad known-parameter and cross-family
controls. The primary must then
freeze numeric priors, sampler/log-density measure, Gaussian proposal and
weights, epsilon schedule, discrepancy, normalized parameter coordinates,
all-particle failure policy, selection rule, exact Qwen prompt, full-estimation
refit, state-only test initializer, source/code/runtime hashes, and a new run
ID in a distinct manifest. Have Luna independently review those gates before
one bounded development run. Only an eligible selected run may reach a
separately reviewed single-use official test scorer. No official test or
scientific breakthrough claim occurred at this wake.
