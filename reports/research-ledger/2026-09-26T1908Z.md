# Research ledger — 2026-09-26T1908Z wake

## Priority and protected outcomes

The Cascaded Tanks methods-control track remains the highest-value unblocked
integration loop. No real-data Qwen proposal, ABC fit, development-target
score, or official test attempt has occurred on this track. Official test
`uVal/yVal` must remain outside model design and development. The Silverbox
multisine confirmation remains consumed after the prior 3/64-particle
nonfinite failure, with no final RMSE. Neither synthetic correctness nor
training-only patterns establish a new scientific mechanism.

The primary read `AGENTS.md`, the original scientific/code/runtime audits,
`reports/research-state.md`, the 14:07Z ledger, the source/inference designs,
and the Silverbox negative-result report. The current tank design still has
unfrozen numeric priors, tolerances, Qwen prompt, real-data inference/scoring
policy, refit/initializer, runtime manifest, and final one-use rule.

## Runtime and source state at wake

A Luna read-only preflight fetched `origin`: clean `main == origin/main` at
`80a3570d69a11d8e2efe81ecd064ed0a6d11f74a`, the prior wake's
status-only commit. This is newer than the content commit currently recorded
in the research-state top checkpoint and should be included in this wake's
sync update. There was no active Python/ABC/SMC research process. The only
relevant process was `omlx-server` PID 15407, listening on local port 8000
at about 21 GB RSS. `GET /v1/models` advertised the exact required
`Youssofal--Qwen3.8-27B-MTPLX-Optimized-Speed` ID; no generation call was
made. The old Silverbox lock named a nonexistent PID 62852 and had no open
holder; the final lock was empty and unheld. The latest Silverbox receipt
remained terminally failed with null final score/target hash.

The official tank ZIP is absent from active checkout but exists in two dated
backup directories; both copies are 7,520,592 bytes and match the pinned
SHA-256 `eb0fa05851e8a7136846c2e3b61fbef87def78d0852c86ab91b02ac5db541b51`.
No duplicate was copied or downloaded. The host reported 16 CPUs, about 70%
idle in a sample, 97% memory-pressure free, zero swap, 430 GiB free disk;
workspace `data` 6.2 MiB and `artifacts` 56 MiB. This remains comfortably
below the 50 GB working-set cap. The old external-repeat source gate is still
failed; only the separately scoped one-test methods-control track is active.

## Synthetic cross-family horizon control

A Luna operator ran a predeclared **synthetic-only**, noiseless H-grid control
and an independent Luna reviewer replayed the equations separately. Ignored
raw evidence is under
`artifacts/evaluations/cascaded_tanks_synthetic_cross_family/` as `run.py`,
`result.json`, `horizon_sensitivity.py`, and
`horizon_sensitivity_result.json`; do not stage these bulk/operator files.
The simulator parameters were fixed at `a=0.5`, `c=0.4`, `p=0.5`,
`x1_0=x2_0=0`; only threshold H varied, with equal four-point H grids and
full-trajectory RMSE budgets for both generated families. Input was 8 for
24 samples, then zero. In a no-crossing control with true H=90 and grid
`[85,90,95,100]`, maximum raw x2 was 82.9124, all O2/C2 observations
matched exactly, and all candidate RMSEs were zero: structure was
unresolvable. In a crossing/recovery control with true H=3 and grid
`[2,3,4,5]`, the first differing observation was `y[47]`, 23 zero-input
samples after the low-input block began. A 14- or 23-sample recovery window
therefore yielded identical observations and zero best cross-family RMSE in
both generating directions. With 54 zero-input samples, O2-generated data
gave the best C2 grid fit full-trajectory RMSE 1.383703; C2-generated data
gave the best O2 fit full-trajectory RMSE 1.761596 over the 78-sample window.
The independent replay found
no discrepancy in settings, candidate counts, trajectories, or scores.
Operator execution was about 0.010 s for the original grid and 0.0045 s
for the horizon follow-up, with approximately 34 MiB peak RSS; reviewer
replay took 0.0021 s at 33.7 MB RSS. No real source archive, hidden target,
Qwen, ABC, or Git was accessed in these controls.

This is **conditional** distinguishability with all non-H parameters fixed,
noise absent, and the true H on the grid. It cannot establish full
parameter identifiability or anything about the real mechanism. The real
training-only EDA had one 14-sample qualifying low-input interval; the
illustrative control needed more than 23 samples before O2/C2 outputs
diverged. That contrast is a warning, not a transfer of synthetic delay to
the tank hardware. A future pre-target forecast-separation/abstention gate
is mandatory; predictive ranking must not be promoted into an overflow
versus sensor attribution.

## Architecture decision and work in progress

An independent Luna code audit found the verified source adapter already
returns exactly training `uEst/yEst[0:768]` and input-only
`uEst[768:1024]`; it has no source-gated ABC bridge or target scorer. The
current simulator's pre-transition output convention means `uEst[767]`
produces latent state 768, which must carry into the 256-step prospective
forecast. The audit warned that the adapter's importable constructor token
is not cryptographic provenance. A future public fit must load the pinned
archive itself and bind source, adapter, code, protocol and run hashes.
The future scorer must require an all-candidates/all-particles-complete
forecast receipt before target access, claim a marker under a lock,
rehash/open the source consistently, and parse only `yEst[768:1024]` while
skipping `uVal/yVal` bytes. It must record null score/target hash when
forecast fails before target access. Source-byte mutations invalidate the
production archive digest even if hidden fields do not affect scientific
fit/forecast payloads; sentinel tests should compare those separately.

Two bounded Luna coders implemented **mechanics only**. The deferred-target
scanner in `core/real_data/cascaded_tanks_deferred_targets.py` accepts the
exact 30,014-byte CSV grammar in a private synthetic seam, buffers and
converts only `yEst[768:1024]` plus first-row `Ts`, and skips the training
suffix and official test columns. Its author passed nine synthetic tests,
Ruff, format, and compilation. A different Luna reviewer inspected the
byte scan and repeated nine tests and Ruff without a blocking finding. Its
failure-gate test is explicitly a **test-only boolean placeholder**: there
is no public archive target reader, complete forecast-receipt gate, or
production scorer yet. This review clears only the synthetic parser seam.

The forecast module in
`core/real_data/cascaded_tanks_development_forecast.py` accepts a
caller-supplied immutable weighted ensemble, loads the official source itself,
and carries all 768 training transitions into the 256-step development
input-only forecast. Any particle failure makes the aggregate forecast and
hash null; `target_sha256` is always null. It performs no fit or scoring and
does not certify that supplied particles came from ABC. Its author passed
five focused synthetic tests, Ruff, format, and compilation. A separate Luna
review found a test-coverage gap: the intended last-input boundary test
initially had `u[767]=0`. The author changed it to a nonzero final input and
checked the recurrence: under pre-transition observation, `u[767]` changes
`x1[768]` and first affects the forecast at `y[769]`, while omitting the
final transition also misaligns `y[768]`. The revised synthetic test asserts
both properties; five focused tests, Ruff, format, and compilation passed
again. The valid production archive path was deliberately not exercised.
An independent Luna reviewer found no correctness blocker in the final diff.
It reran five focused tests and Ruff offline and passed compilation; the
scoped forecast seam is cleared for synchronization, not for real fit or
target scoring. The reviewer noted the residual hostile in-place mutation
during the source adapter's same-descriptor hash/parse is inherited from
that adapter, not introduced by this wrapper.

The controlled source loader verifies the pinned archive hash and parses
from the same open descriptor. The initial forecast wrapper redundantly
reopened the path for another preflight; independent review identified this
unnecessary non-atomic comparison, and the author removed it. The final
wrapper loads through the controlled source adapter and recomputes its
track, contract, archive, and stage-receipt checks. No target is read.
Neither mechanics component is permission to run Qwen or ABC on tank data.
Next freeze and independently review the full
numeric protocol and source/code/runtime manifest, then implement the
source-gated inference bridge and all-candidate deferred scorer.
