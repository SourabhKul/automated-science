# Research ledger — 2026-09-26T0906Z wake

## Priority and protected evidence

The Silverbox v2 development fit/selection completed, but its one locked
multisine confirmation failed with three nonfinite particle forecasts and no
RMSE. That test condition remains consumed. Do not use its input, retry it,
remove failed particles, or treat the development margin as discovery. The
highest-value unblocked work is an independently sourced second-system
**methods control**, with a protected official test and no new model run until
its adapter and inference protocol are reviewed.

## Actual operational state

A Luna read-only preflight found no active Python, ABC, or Silverbox job. The
Silverbox lock files were not held by an OS lock. One `omlx-server` process
listened on `127.0.0.1:8000`; `GET /v1/models` returned HTTP 200 and the exact
`Youssofal--Qwen3.8-27B-MTPLX-Optimized-Speed` ID. No generation request or
simulation was made. Before this wake's edits, `main` was clean and matched
`origin/main` at `6b7d7ab63c8d00f7765a332fea0c7327908b124c`. The host
reported 16 CPUs, 128 GiB RAM, and about 430 GiB free disk. The workspace
`data` and `artifacts` occupied about 6.2 MB and 56 MB. Later model work, if
separately approved, should retain a 600-second/2-GiB bound unless observed
capacity or task needs justify a newly frozen budget.

## Source-contract resolution, not a new scientific claim

The historical `data/real/cascaded_tanks/manifest.json` marked Cascaded Tanks
`source_contract_failed`: it treated `uVal/yVal` as validation and found no
third external-repeat record, prohibiting a Phase 23 discovery campaign. A
Luna source audit checked the
[versioned 4TU record](https://data.4tu.nl/articles/dataset/Cascaded_Tanks_Benchmark_Combining_Soft_and_Hard_Nonlinearities/12960104/1),
[official loader source](https://github.com/MaartenSchoukens/nonlinear_benchmarks/blob/f9fb3883086870a27b31917ccde1f78c95d53cb2/nonlinear_benchmarks/benchmarks.py#L113-L136),
[official reporting rules](https://www.nonlinearbenchmark.org/results-reporting),
and [original benchmark manuscript](https://cris.vub.be/ws/portalfiles/portal/24190624/CascadedTanks_Maarten.pdf).
The loader maps `uEst/yEst` to `train_val` and **`uVal/yVal` to the official
test**, with 50 permitted test-initialization samples. The benchmark supplies
one 1,024-point estimation record and one 1,024-point test record at 4-second
sampling; no native third repeat exists. Both statements can coexist: the old
multi-repeat gate still fails, while a separately named one-test methods
control is feasible. The historical manifest is preserved verbatim.

The primary drafted
`reports/cascaded-tanks-source-resolution-proposal-2026-09-26.md`, track ID
`cascaded_tanks_methods_v1_20260926`, to supersede the historical manifest's
unscoped prohibition **only** for a bounded target-deferred adapter and
methods-control development workflow. An independent Luna reviewer found no
remaining source or methodological blocker for that adapter gate, but did not
approve fitting, Qwen/ABC, or final scoring. The proposed `[0,768)` train and
`[768,1024)` internal validation split still needs a frozen scoring and
initialization decision before a run. It is one correlated realization, not an
independent repeat. A later refit on all 1,024 estimation points is proposed
only after structure/protocol choices freeze. The official test would be
single-use, and the public-data blind is procedural, not secret.

The [2016 benchmark manuscript](https://cris.vub.be/ws/portalfiles/portal/24190624/CascadedTanks_Maarten.pdf)
already describes the two-tank outflow and stochastic overflow, uncalibrated
lower-level voltage sensor, and unknown initial states.
[Prior SMC work](https://arxiv.org/abs/2210.14684) already applies sequential
Monte Carlo to a two-tank overflow system. The
[current benchmark baselines](https://www.cambridge.org/core/journals/data-centric-engineering/article/benchmarking-for-nonlinear-system-identification-submission-platform-and-baseline-results/BD06B2C87096B4A700EE173A298A0FEC)
make this a useful independent system-ID control, not a plausible claim to
discover the known tank mechanism. Published comparisons require matched
initialization, free-run metric, source split, and training-data allowance.

## Archive and resources

An operator found the already acquired archive in a dated local backup, not
under this checkout's active `data/real/cascaded_tanks/raw/`. It is 7,520,592
bytes; its MD5 `f832776ed1efdca4572338f467b7baec` matches the published
4TU checksum and its SHA-256
`eb0fa05851e8a7136846c2e3b61fbef87def78d0852c86ab91b02ac5db541b51`
matches the historical manifest. ZIP central-directory metadata gives
7,625,951 total expanded member bytes, no traversal or duplicate names, and
no size-ratio alarm. The operator did not open any archive member or outcome,
extract/copy/download the archive, or run a model. Workspace data plus
artifacts plus that existing archive is about 69.8 MiB, below the 50 GB cap.
The separate official record's "11,676,168 bytes unzipped" is an attachment
total, not the ZIP-member expansion size.

## Decision and next gates

A Luna implementer is building only the development source adapter against
synthetic fixtures: strict source/header/size checks and column-limited
`uEst/yEst` parsing, with validation/test outputs absent from returned
objects. No real archive member is to be read during this implementation
step. Next: independent code review and synthetic leakage tests; only then a
read-only real source smoke that accesses estimation fields but not official
test outcomes. Freeze the exact model, ABC, validation/refit/final rules and
runtime manifest with separate review before any Qwen, ABC, or final attempt.
No new research run, manuscript submission, external message, purchase, or
financial transaction occurred this wake.

## Later in this wake: adapter implementation, failed smokes, and source pass

Luna implemented `core/real_data/cascaded_tanks_controlled.py` and focused
synthetic tests under track `cascaded_tanks_methods_v1_20260926`. An
independent review caught that the first public API could accept an arbitrary
archive expectation while still labeling the result official. The author
removed public overrides, separated synthetic fixture types, bound production
receipts to the pinned archive SHA, and made official data construction
loader-only. A second independent review approved that repair. The adapter
never returns `uVal`, `yVal`, or validation `yEst` targets in development;
the fixed source checksum, archive size, CSV bounds, row counts, training
split, and typed receipt semantics are checked. Tests include mutations of
the hidden fields that leave the development view and receipts unchanged.

Three versioned real-source smoke attempts stopped at source-format checks:

1. Code SHA-256 `e14faba9e094433dc861cb334cdd804c5ee76222d51a0ac3ce84ef9412b242a9`
   rejected the legitimate `CascadedTanksFiles/` directory entry at ZIP
   preflight; zero CSV rows were parsed.
2. After a reviewed directory fix, code SHA-256
   `caa32c65995e9318dc97e1f4da389426c02cbb67f3c34721347818d8c38481a8`
   passed preflight but rejected the real quoted/trailing-comma header; zero
   CSV data rows were parsed.
3. After a reviewed wire-header fix, code SHA-256
   `967ced963a3077bb869d5a556eb4cfd06be091a209335eff2339befeba3726f2`
   passed preflight and header but found `Ts` empty on logical row 1; no
   development object was returned.

Operators made no immediate retry on any failure. Separate format-only audits
identified the exact quoted/trailing-comma header, 1,024 regular rows with
an empty sixth field, `Ts=4.0` only on row 0 and blank thereafter, and one
extra final empty physical line. The full-structure audit streamed the CSV
without retaining or emitting measurement values; the two preceding script
invocations failed before processing data rows (one unavailable `python`
executable, one scanner bookkeeping error). Its corrected bounded stream
completed in 0.006 s. This is source-format evidence, not model evidence.
The adapter author then pinned the exact 30,014-byte CSV wire grammar and
added equal-size malformed fixtures; an independent reviewer approved after
**39 focused synthetic tests** and Ruff passed. These source-format failures
and repairs are retained rather than relabeled as a first-try pass.

One subsequent bounded production source smoke succeeded under adapter
SHA-256 `d651c1a149c702342498ecc63d13c47a803e424fb93938225c5f71cf4b949014`,
contract SHA-256 `c92dad6bcf731c1171172861ca85fae5d9b69dff5a45783b61f3eeeeda017723`,
and source archive SHA-256
`eb0fa05851e8a7136846c2e3b61fbef87def78d0852c86ab91b02ac5db541b51`.
It returned 768 `uEst` training inputs, 768 `yEst` training outputs, and 256
`uEst` development inputs, `Ts=4.0`; it returned no target suffix or official
test fields. Training and forecast-input stage receipt SHA-256 values are
`0eadc6c62e7f4e3dea76311dff037a7fee1c21fda6fac20e9363310dcf900d61`
and `dbffbfc30b0b2ae9a0f7b48a850e57f7113909c50bc8fcb6d84a109e7508c486`.
One public preflight and one public development load were called. The source
smoke took 0.043 s total, observed peak RSS about 3.9 MB, below its 60-s/
1-GiB bound. The parser scanned record bytes but did not buffer, decode, hash,
or numerically materialize hidden columns or validation target suffix. No
Qwen generation, ABC-SMC fit, validation score, or final test took place.

Next gate: synchronize only the reviewed adapter/tests and compact reports,
then freeze the tank-specific model/observation, prior, ABC distance/epsilon,
development selection and full-estimation refit protocol plus provenance
manifest. That protocol needs independent review before a model run. The
official test remains unscored and must not inform choices.

## Later in this wake: reviewed sync and development-only exploration

The sole Luna Git owner fetched upstream, fact-checked the source proposal,
adapter code/contract hashes, ledger, and state, reran the 39 focused tests,
Ruff and compilation, and pushed **only** the five reviewed code/test/report
files as commit `493e33bec0c31a33b9748d1c747dcaa742cbbd11` on `main` without
force. A fresh fetch verified `HEAD == origin/main` and a clean worktree at
that commit. No raw archive/member, receipt, secret, environment, or bulk
artifact was staged. Later compact status additions require a separate sync.

Two Luna operators independently computed a small descriptive analysis using
only the public adapter's returned training `uEst/yEst` indices `[0,768)` and
later `uEst` **input** indices `[768,1024)`. Both found all returned values
finite. Training input min/median/max was 1.2099/2.7048/6.4712 V, population
SD 0.9246574 V; training sensor output was 2.9116/4.9946/10.0 V, population
SD 1.9646730 V. The later development input alone was
0.40937/3.11365/5.3176 V, population SD 1.1866354 V. Exactly 228/256 later
inputs were within the inclusive training-input min/max, and only 8/10 equal-
width training-amplitude bins were occupied by them; the two highest bins
were empty. The possible output ceiling at 10 V is a hypothesis, not evidence
that sensor clipping or tank overflow caused it. The proposed 768/256 suffix
therefore probes a lower-input excursion and does not cover all training
amplitudes; it cannot be treated as a representative independent record.

For exploratory lag description, each operator computed overlap-specific
Pearson `r(u[t],y[t+k])` on training pairs only for `k=0..64`, recomputing the
two means for each overlap of size `768-k`. The peak was `k=28` (112 s),
`r=0.7523215340` on 740 overlaps, with nearby lags 27–30 also high. Strong
autocorrelation in both series makes this **not** an identified physical
delay. Hidden upper state, sensor calibration, process noise, and a single
multisine record remain confounders. These summaries may guide scale and
synthetic controls, but they are development evidence, not a fitted model,
ABC posterior, independent validation, or discovery.

The analyst's read+analysis took 0.037 s with 27.2 MB observed peak RSS; the
independent reproduction took 0.021 s with 28.1 MB peak RSS. A 55-s CPU/wall
cap was set. macOS rejected a 1-GiB address/data-space rlimit before source
loading because the process reserves large virtual mappings; physical RSS was
observed below 1 GiB but was not enforced by that rlimit. No validation
`yEst` suffix, official `uVal/yVal`, Qwen, ABC, simulation, or score was used.
The next inference protocol must freeze its split, initializer, priors,
calibration, measurement model, full-train refit, and one-test gate with an
independent review before a model run.
