# Observation mismatch and abstention control pilot ledger

Date: 2026-09-18. Run ID: `20260918T110738Z`.

The independent review approved exactly one frozen serial six-cell pilot. A
fresh preflight found `HEAD=origin/main=029b1e2fb592c774041ce7b30240db2854bce25f`,
no competing research workloads, 16 CPU cores, 128 GiB RAM, and 447 GiB free
disk. Read-only GET `/v1/models` returned HTTP 200 and advertised the fixed
Qwen model `Youssofal--Qwen3.8-27B-MTPLX-Optimized-Speed`; no model generation
was requested.

The driver used the predeclared A/B cells with data seeds `11`, `29`, and `47`,
the fixed grid/split/sigma/margin/future-time settings, and 512 predictive
draws. It ran serially in 6.8 seconds, below the 540-second cap. All 6/6
cells completed and every model receipt was complete with normalized weights,
finite ESS, counters, seeds, solver/observation configuration, and retained
alternative forecasts. Raw receipts remain ignored under
`artifacts/observation_mismatch_control/`; the atomic manifest SHA-256 is
`1cd0c016cfd4c74ae3f16dcd62c837590e057ab3065370377cd18bbb184dc3f1`.

Experiment A selected the correct saturating observation map for seeds 11 and
47, with validation improvements `0.0480835` and `0.0309933`, and evaluated
the selected model once on the sealed suffix. Seed 29 recorded the valid
`unresolved_retain_both` outcome and did not run a sealed score. Experiment B
recorded `unresolved_retain_simpler` for all three seeds. Seed 47 passed the
validation margin `0.0182601 >= 0.0176777` but failed sealed reproduction
(`-0.0099252`), preserving both forecasts and abstaining. All fixed future
candidate disagreements were below `2*sigma`, so every B cell reported
`no_informative_measurement_under_this_protocol`.

This is known-simulator control evidence only. It makes no real-data,
mechanism-discovery, model-probability, coverage, novelty, or intervention
claim. No rerun, setting change, commit, or push was made after the pilot.
