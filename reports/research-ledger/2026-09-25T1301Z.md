# Research ledger — 2026-09-25T1301Z

## Wakeup and preflight

Read the standing guidance, initial scientific/code/runtime audits, current
research state, and real-data plans. `HEAD` and `origin/main` both resolved
to `a8b43477e82df7c323fc30b109a0d1a8a38e6215`; the worktree was
clean. The only relevant process was the existing `omlx-server`; there was
no ABC, Silverbox, or canonical research job or recent checkpoint to resume.
A delegated read-only `GET /v1/models` returned HTTP 200 and included the
exact fixed Qwen ID. No Qwen generation occurred.

## Training-only exploration

A Luna operator used only the four fixed training windows of the controlled
development loader, never its validation field or the scorer-only loader.
There were 824 predicted-step rows. A forced observed-output linear AR(2)
ordinary least-squares fit yielded
`(a1,a2,b,c)=(1.4631070,-0.9361293,0.4190676,-0.00213506)`;
one-step residual RMS was 0.00112682 and four-window free-run RMS was
0.00719364. Its linear poles had maximum modulus 0.967538. A cubic
`y[t-1]^3` term fitted on the same training rows had coefficient -1.4802923,
one-step RMS 0.000962123, and free-run RMS 0.00482064; its raw design
condition number rose to 2523.90. These are development descriptive fits,
not validation scores, posterior evidence, independent samples, or a
physical-mechanism finding.

Linear one-step residual correlations with candidate features `y^3`, `u^3`,
and `u*y` were -0.290589, +0.0503233, and +0.0669048, respectively. These
diagnostics and training ranges motivated a proposed frozen one-Qwen,
two-family checked-ABC protocol in
`reports/silverbox-first-fit-protocol-2026-09-25.md`. The protocol is under
independent review and has not been executed. Validation and sealed suffix
targets remain unseen. The proposed search supports are deliberately
training anchored and therefore will not be described as calibrated
Bayesian priors or model probabilities. No new science claim follows from
these pilot controls.

An independent Luna review initially requested explicit forecast-summary,
failure, scorer access, prompt/JSON, and receipt rules. The primary agent
revised the protocol; independent re-review **approved it for
implementation**, while requiring separate code and scorer review before
any run. Two Luna max workers now own non-overlapping implementation scopes:
one exact Qwen proposer/parser/receipt component and one controlled
checked-ABC development fit/validation component. Neither is authorized to
make a live Qwen request, fit Silverbox, access validation outcomes, or read
sealed outputs during implementation. The final scorer is a later gate.

The two components were implemented in separate files and independently
reviewed. The proposer now requires a fresh exact-ID `/v1/models` preflight,
then at most one fixed-payload POST; it rejects non-assistant content,
duplicate/trailing JSON, truncated responses, and model mismatch, with an
atomic ignored receipt whose exact SHA-256 is returned for downstream
binding. The fit accepts only fixed training windows, an explicit selected
term, and that proposer receipt hash; it uses the checked ABC reference and
exposes validation selection only as a later call. The joint focused suite
passed 29 tests; compilation passed. The independent reviewer approved both
for their bounded scope. No live Qwen request, ABC fit, validation score, or
sealed access occurred. A reviewed orchestration bridge still must verify
that the proposer receipt is successful and its term matches the fit, and a
separate multisine-only scorer must be built before any final result.

The fit's mandatory memory guard imports `psutil`, which is absent from
`requirements-freeze.txt` and from the default checkout environment. In this
sync environment, the focused suite initially exposed that dependency gap:
without `psutil`, the guard correctly marks the fit incomplete before
calibration, causing the two tests that expect monitored fit behavior to fail.
The same focused suite passed 29/29 when run once in an ephemeral `uv`
environment supplying `pytest`, `numpy`, and `psutil`. No dependency manifest
was changed here. A reviewed environment/dependency integration must make
`psutil` available before any real pilot; missing memory monitoring must
continue to stop the fit rather than silently relax the guard.

## Next gate

Resolve any methodological review blockers, freeze the exact prompt and
implementation, independently review code and tests, then run only the
bounded development fit. The scorer remains gated behind a locked
development decision. Raw research data remain ignored and the active
downloaded/expanded Silverbox set is about 15.5 MB, below the 50 GB cap.
