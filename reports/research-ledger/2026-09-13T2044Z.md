# Heartbeat checkpoint: 2026-09-13 20:44 UTC

This checkpoint covers the scoped canonical train/validation/sealed-final boundary work. The workspace and active workloads were inspected before execution. `GET http://127.0.0.1:8000/v1/models` returned HTTP 200 with the fixed research model `Youssofal--Qwen3.8-27B-MTPLX-Optimized-Speed`; the only pre-existing matching process was `omlx-server` PID 47976. No domain, sandbox, or final-evaluator worker was running.

The implementation uses the existing `scripts.run_domain` -> `core.sandbox_eval` path behind the explicit `trajectory_train_validation_sealed_v1` protocol flag. Development manifests expose train and validation only; validation drives canonical selection while final inputs stay in a separate sealed manifest. Frozen selection binds code, parameter semantics, parameters, initial conditions, solver settings, original time origin, and the development receipt. `scripts/run_final_evaluation.py` verifies those bindings, loads final data independently, integrates from frozen `t0/y0`, and writes a terminal receipt. Sentinel, cross-protocol, time-origin, overflow, non-finite prediction, and prompt/input separation tests pass. The legacy `--held-out` path remains adaptive validation, and no claim is made for grouped adapters or OS isolation.

Focused validation after the final helper and budget fixes:

```text
pytest tests/test_evaluation_boundary.py tests/test_sbi_engine.py tests/test_run_domain_config.py: 11 passed in 4.02s
compileall: passed
git diff --check: passed
```

The bounded real-Qwen smoke used the exact model and endpoint, `enable_thinking=false`, seed 17, `max_tokens=256`, two proposal slots, and a temporary synthetic 60/20/20 trajectory. The run tag was `canonical_qwen_boundary_smoke_20260913_ecology`. The seed froze with development validation MSE `71.36812249334737`. Two Qwen proposals and two repairs were syntax-invalid, so no proposal candidate was evaluated. The 256-token cap may have contributed to truncation; finish reasons were unavailable, so this remains a possible cause only. The actual request count was four (two proposals plus two repairs), exceeding the intended total allowance of two proposals plus one repair because the pre-fix cap was per proposal; this overrun is preserved in `reports/evaluation-boundary-smoke-2026-09-13.md`. No Qwen rerun was made. Response token usage was unavailable from the existing client path.

The one separate final evaluation made no model request and returned `status=success` for the frozen seed incumbent, with finite final MSE `126.09971677703511`. Its receipt verified the development receipt and sealed-manifest hashes and recorded the final receipt hash. This is synthetic protocol/runtime evidence only; it is not a scientific performance or calibration claim. All raw run artifacts remain ignored under `models/` and temporary manifests/receipts remain under `/tmp`.

Current process check after final evaluation: only the pre-existing `omlx-server` remained; no experiment worker was active. A durable ignored evidence copy, with recipe and 21-file SHA-256 inventory, is at `artifacts/evaluation_boundary/canonical_qwen_boundary_smoke_20260913/` (manifest SHA-256 `1bdf9cd7fc3f2e73efafdecc0e6d72e7733e4e29187ef14e407b8bb47e2c33a5`). No commit or push was made in this checkpoint pending parent and independent review of the final scoped diff.
