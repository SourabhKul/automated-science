# Research ledger — 2026-09-25T0301Z

## Phase 1 boundary synchronization checkpoint — 2026-09-25T0808Z

Before synchronization, local `main` and fetched `origin/main` both resolved
to `191a25d7daa7c2af7fe2679a16cf516aedf2480f`. The approved Phase 1 source,
tests, plan, ledger, and research-state update were the only intended Git
scope. The process scan found no active Git synchronization, Qwen, ABC, fit,
or Silverbox job.

The independently approved `core/real_data/silverbox_controlled.py` boundary
and `tests/test_silverbox_controlled.py` passed the exact focused command
`uv run --with pytest --with numpy python -m pytest -q
tests/test_silverbox_controlled.py` (5 passed). These focused tests construct
a temporary index-coded archive. No raw archive or sealed test outputs were
accessed in this verification. Compilation and staged-diff checks were
completed as part of this synchronization.

The module passes measured input, the 50-sample initialization output,
parameters, and native sampling time into its simulator call; it does not pass
`target_y`. However, `validation.target_y` remains accessible to downstream
candidate selection. Phase 1 does not enforce the policy that validation
metrics cannot set fit parameters or ABC thresholds. That policy and an
auditable train-fit/validation-selection receipt remain future pre-fit work.
This is a controlled data/simulator boundary only: no end-to-end leakage-proof
claim, fit, real-data inference result, sealed evaluation, or discovery claim
is supported.

The canonical runner still uses its autonomous legacy `SBIEngine`, and this
module has not been connected to the checked ABC-SMC reference or a scorer.
The model family, priors, train discrepancy, ABC schedule, and practical
selection margin remain unfrozen. No Qwen generation or fitting was run.

## Verified checkpoint

`HEAD` and `origin/main` both resolve to
`191a25d7daa7c2af7fe2679a16cf516aedf2480f`; the worktree was clean
after the reviewed Silverbox source-contract, milestone plan, and GPT-6 Luna
delegation sync. The content commit is
`35831429ea8c1e23d6c7804e333223fdcc71dc8e`. No research experiment
was running at the start of this wake.

The previously stopped oMLX service was restored with one bounded start of
the existing installed service. A subsequent `GET /v1/models` returned HTTP
200 and advertised the exact
`Youssofal--Qwen3.8-27B-MTPLX-Optimized-Speed` ID. The local checkpoint
files were already present. No generation or model download was performed.
The service was running at the time of the recovery check; future runs must
repeat the exact-ID preflight.

## Interface finding and scientific decision

An independent read-only integration audit found that the official Silverbox
adapter exposes both measured input and output in development data, whereas
the canonical trajectory evaluator passes only time and output to its
autonomous simulator. That evaluator also still calls the historical
`SBIEngine`, not the corrected opt-in ABC-SMC reference. Therefore a
Silverbox pilot cannot be scientifically credited as an integrated
Qwen-plus-checked-ABC result through the current canonical runner.

At that checkpoint, the next engineering gate was a controlled-system path
that carried measured input, development-only outputs, and frozen source
indices into the checked `core.abc_smc_reference` sampler. It needed an explicit observation model and
state initializer, a train-only discrepancy, a validation decision, and a
scorer-only locked final path. The official test multisine contains 21,688
samples and permits only the first 50 measured outputs for initialization.
The 32,000-sample arrow subset overlaps the 40,475-sample full arrow record;
they cannot be counted as independent confirmations.

## Budget and limits

The host inspection found 16 logical CPUs, 128 GiB physical memory, and
roughly 434 GiB free disk. A full-series 150,000-particle tensor would be
far beyond the intended pilot memory budget. The first implementation and
pilot should use a small fixed development window, two low-dimensional
candidate families, bounded ABC populations and attempts, wall-time and
memory guards, and atomic incomplete receipts. This is an engineering
bound, not an inference-calibration claim. No development split, priors,
epsilon schedule, candidate family, or sealed score has yet been frozen.

The Silverbox source audit reports 15,532,542 bytes raw plus expanded and
records that its CSV values differ slightly from the official MAT arrays.
Dataset reuse terms are unstated. Those limitations remain part of any later
methods claim. This checkpoint establishes service and interface readiness,
not a scientific discovery or a real-data result.

## Development-only characterization and implementation handoff

A Luna operator used only the default development loader and read no sealed
candidate views or scorer-only records. The published train/validation
record's 65,062 samples are finite; input RMS is 0.0230440 and output RMS
is 0.0545493. In the planned `[40650,48842)` block, input RMS is 0.0231833
and output RMS is 0.0549847. The planned training interval's output RMS is
0.0565432; validation's is 0.0502704 and its range is narrower. Four
preselected 256-sample training windows cover 65.2–87.4% of the training
input peak-to-peak range each. No obvious missingness, clipping, or extreme
outliers appeared under the operator's finite/plateau/MAD checks. These are
development descriptive facts, not evidence of model quality.

The primary agent wrote
`reports/silverbox-controlled-development-contract-plan-2026-09-25.md`.
It fixes the first data block, role split, unused gap, training windows,
native sampling, and initialization semantics. It explicitly defers the
model families, Qwen proposal grammar, priors, ABC thresholds, and practical
selection margin to a second reviewed pre-fit freeze; none can be selected
from sealed outcomes. A GPT-6 Luna max implementation agent was assigned
only the controlled development boundary and meaningful tests. No fit or
generation has been authorized by that bounded implementation assignment.
