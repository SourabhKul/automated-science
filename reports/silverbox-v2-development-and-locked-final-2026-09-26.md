# Silverbox v2 development and locked final attempt — 2026-09-26

**Outcome:** the fixed-Qwen plus checked ABC-SMC workflow completed its train-only fits and selected the nonlinear `y_cubed` candidate on validation. Its one permitted multisine final attempt then failed because three of 64 weighted particles generated nonfinite trajectories. No final RMSE or target hash exists. This is a reproducible methods-control failure, not a new physical-science result.

## Frozen question and provenance

The preceding v1 run was unresolved after exhausting 512 second-population attempts; it produced no validation score. The separately reviewed v2 protocol increased the maximum to 2,048 attempts per population for **both** linear and nonlinear families while retaining 64 accepted particles/population, 256 prior calibration draws/family, q50/q20 train-only thresholds, the checked reference proposal/weight kernel, supports, seeds, and the frozen validation/final rules. It was an adaptive, training-informed budget follow-up. The original v1 evidence is preserved.

V2 run ID: `silverbox-20260926T050458Z-03859cef3530`. Reviewed Git revision and runtime pinned before generation: `0db503edb9ec33dc1d831b9306cd40959b3de41a` on a clean `main`, Python 3.12.11, NumPy 2.4.4, psutil 7.2.2. The ignored exact-byte v2 manifest has SHA-256 `b830357ba4225621d84193bf1bea7b788ac426e75d48d54645e2391db397a26f`; a separate Luna reviewer recomputed all eight code hashes, source identity, runtime, Git state, and manifest hash before launch. The official `SilverboxFiles.zip` was 5,793,999 bytes with SHA-256 `2398dda503ac4c30f46c7a1ba09d8a8864d001cc71bbeb59f207a054cc592d8c`. Raw archive and run receipts remain ignored local artifacts.

The source contract used four fixed 256-sample training windows within `[40650,48842)`, validation `[47050,48842)` with a 50-sample measured initializer and 1,742 forecast targets, and one locked `test_multisine` interval `[105712,127400)` with a 50-sample initializer and 21,638 prospective target values. Earlier Arrow-test rows were not used for training or final scoring. The exact Qwen model was `Youssofal--Qwen3.8-27B-MTPLX-Optimized-Speed` on local oMLX. One successful proposal selected `y_cubed`; its response SHA-256 was `ad97d569dcc962c37ff65a84a64f9cffd6f2308e3085bcf5e599f1a173fa0e8d`. This repeats the v1 term under the same seeds, but same-stream equivalence has not been verified; it is not an independent replicate.

## Development result

Both train-only calibration ledgers completed 256 draws. Linear had 222 finite draws; nonlinear had 187. Both first and second ABC populations completed with 64 accepted particles. Second-population attempts were 553 linear and 773 nonlinear, below 2,048; out-of-support proposals were 377 and 574, and simulator failures were 2 and 18. Terminal ESS was 55.6702 linear and 56.9518 nonlinear. An independent Luna audit recomputed calibration quantiles, support membership, covariance, proposal mixture densities, normalized weights, ESS, counters, and pointwise weighted-median validation forecasts from the persisted receipts; it found no discrepancy. This does not establish posterior calibration or physical identifiability.

The frozen validation rule required both model forecasts to beat persistence, then promoted nonlinear only for at least a 5% RMSE reduction relative to linear. Validation RMSE was 0.0193194470 linear, 0.0182095781 nonlinear, and 0.0601643587 persistence. The nonlinear reduction was 5.7448%, so the protocol selected nonlinear. This is one development block from one device, with a margin only 0.7448 percentage points beyond the arbitrary 5% threshold. It is not independent evidence of a mechanism. The development run completed in 4.334 seconds under the 600-second cap; peak additional fit RSS was 4,587,520 bytes under the 2 GiB cap. Fit and selection content hashes are `14cccea389073ca1147bbe7af0a1e1365689e8539bf840b5a1f2c8fbf67be9e4` and `6d691747561f826eb118a7c4645ae85a64743ef166368d89689d1a34813658a7`.

## One final attempt and its limit

After two read-only audits confirmed the selected terminal receipt and absence of any final marker, a separate Luna operator invoked the reviewed scorer **once**. Its input was the official multisine record, not the overlapping Arrow records. The final started marker content hash is `35b2536870711d625f4fe93862216875665b02d04e86148a0f65d42a2551db8d`; the terminal final receipt content hash is `212f971c941d033c0d4381365a3f0a3c79843bf883dc52529ab1ac23b7a886ca`. The latter records `status=failed`, `failure_mode=particle_simulation_failure`, and zero-based nonlinear particle indices **25, 44, 53** as `nonfinite_trajectory`. The protocol forbids dropping failed particles or renormalizing the remaining ensemble. It therefore recorded **no weighted-median forecast, no final RMSE, and null target/forecast hashes**. The scorer had read the multisine input and permitted initializer (SHA-256 `691b8295c255587bdaa16fa1e492e316d75c6305687c881a661a83a8f1c11c28` and `ed5eaa8e98172fb6ee41b35f5af283ed30f0f45b4b167c7976550b3d36088dfa`); it returned before numeric target-suffix materialization. The CSV reader may have transiently tokenized suffix-output strings while scanning input rows, so this is a code-path/receipt claim rather than a runtime proof that no suffix bytes entered process memory. The 1.434-second attempt is terminal and single-use. Independent receipt audits found one start marker and one final receipt, with no second scorer call or active process.

This failure shows that the *locked selected particle ensemble* could not produce a complete forecast under this multisine input. It does not show which mechanism is true, whether the linear family would have predicted better, or any final RMSE. The failure could reflect model instability, numerical behavior, inadequate constraints, or a regime shift; this attempt does not distinguish those causes. The multisine condition is consumed for confirmatory purposes. Do not remove the three particles, change the divergence rule, tune on that input, or rerun this test as if it were untouched. Future stability diagnostics may use development data; a revised method needs a new independent confirmation condition or dataset.

## Reproducibility and operational notes

Ignored evidence directories: `artifacts/silverbox_first_fit/silverbox-20260926T050458Z-03859cef3530/`, `artifacts/silverbox_first_fit/_orchestration/silverbox-20260926T050458Z-03859cef3530/`, and `artifacts/silverbox_first_fit_v2/`. The first attempted v2 CLI invocation had a mistyped protocol ID and failed in argparse before workflow start or Qwen dispatch; its operator log is retained. A later final precondition script expected the selected term directly in `run_finished.json`; it stopped before calling the scorer and was corrected to read the content-hashed selection receipt. Exactly one actual v2 workflow and one actual scorer invocation occurred. No code, thresholds, priors, seeds, or data split were changed in response to their outcomes.
